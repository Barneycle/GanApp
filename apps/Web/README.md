# React + Vite

This template provides a minimal setup to get React working in Vite with HMR.

Linting is handled by [Oxlint](https://oxc.rs/docs/guide/usage/linter.html). Run `npm run lint` or `npm run lint:fix`.
